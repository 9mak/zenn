name: Auto Publish Specific Article

on:
  push:
    branches:
      - develop

jobs:
  auto-publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        with:
          fetch-depth: 0 # リポジトリのすべての履歴をフェッチ

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install PyYAML
        run: pip install pyyaml

      - name: Get list of changed files
        run: |
          git diff --name-only ${{ github.event.before }} ${{ github.sha }} | grep 'articles/.*\.md$' > changed_files.txt || true

      - name: Update 'published' status for changed articles
        run: |
          while IFS= read -r file; do
            if [ ! -f "$file" ]; then
              echo "$file does not exist."
              continue
            fi
            echo "Processing $file"
            echo "Before sed:"
            cat "$file"
            sed -i '' 's/^published: false$/published: true/' "$file"
            echo "After sed:"
            cat "$file"
          done < changed_files.txt

      - name: Commit and push if changes are detected
        run: |
          git config --global user.name 'GitHub Action'
          git config --global user.email 'action@github.com'
          git add .
          git commit -m 'Auto-publish articles' -m '[skip ci]' || exit 0  # Exit gracefully if no changes
          git push --set-upstream origin HEAD:develop --force
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

